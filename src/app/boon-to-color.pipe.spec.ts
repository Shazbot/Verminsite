import { BoonToColorPipe } from './boon-to-color.pipe';

describe('BoonToColorPipe', () => {
  it('create an instance', () => {
    const pipe = new BoonToColorPipe();
    expect(pipe).toBeTruthy();
  });
});
