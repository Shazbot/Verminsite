import { KeysToKeyIconsPipe } from './keys-to-key-icons.pipe';

describe('KeysToKeyIconsPipe', () => {
  it('create an instance', () => {
    const pipe = new KeysToKeyIconsPipe();
    expect(pipe).toBeTruthy();
  });
});
