import { DifficultyToSkullsPipe } from './difficulty-to-skulls.pipe';

describe('DifficultyToSkullsPipe', () => {
  it('create an instance', () => {
    const pipe = new DifficultyToSkullsPipe();
    expect(pipe).toBeTruthy();
  });
});
